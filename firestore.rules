rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to photos collection
    match /photos/{document} {
      allow read, write: if true;
    }
  }
}
